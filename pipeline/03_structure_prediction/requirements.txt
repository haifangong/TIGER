# HelixFold / structure prediction dependencies
absl-py>=0.13.0
biopython>=1.79
dm-tree>=0.1.6
immutabledict>=2.0.0
ml-collections>=0.1.0
numpy>=1.20
pandas>=1.3
scipy>=1.7
# Install a CUDA-compatible paddlepaddle separately, e.g.:
#   python -m pip install paddlepaddle-gpu==...
# PyRosetta is required only for relax.py and is usually installed via the rosetta conda env.
